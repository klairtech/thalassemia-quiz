# Thalassemia Quiz App Environment Variables
# Copy this file to .env.local and fill in your actual values

# ===========================================
# SUPABASE CONFIGURATION
# ===========================================
# Get these from your Supabase project settings
# 1. Go to https://supabase.com
# 2. Create a new project or select existing one
# 3. Go to Settings > API
# 4. Copy the Project URL, anon key, and service role key

# Public URL (safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anon key (safe for client-side with RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Service role key (NEVER expose to client - server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# ===========================================
# GOOGLE TRANSLATE API
# ===========================================
# Get this from Google Cloud Console
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing one
# 3. Enable Cloud Translation API
# 4. Go to APIs & Services > Credentials
# 5. Create an API Key
# 6. (Optional) Restrict the key to your domain

GOOGLE_TRANSLATE_API_KEY=your-google-translate-api-key-here

# ===========================================
# APP CONFIGURATION
# ===========================================
# Your app's base URL (for development use localhost, for production use your domain)

NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================
# These are optional and have default values

# Database connection pool size (default: 10)
# DATABASE_POOL_SIZE=10

# Translation cache duration in seconds (default: 3600 = 1 hour)
# TRANSLATION_CACHE_DURATION=3600

# Maximum quiz questions per session (default: 3)
# MAX_QUIZ_QUESTIONS=3

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env.local to version control
# 4. For production, set these variables in your hosting platform

# ===========================================
# SUPABASE SETUP STEPS
# ===========================================
# 1. Create Supabase project at https://supabase.com
# 2. Run the SQL from supabase-schema.sql in SQL Editor
# 3. Run the SQL from seed-data.sql to populate questions
# 4. Copy your project URL and anon key to the variables above

# ===========================================
# GOOGLE TRANSLATE API SETUP STEPS
# ===========================================
# 1. Go to Google Cloud Console
# 2. Create/select project
# 3. Enable Cloud Translation API
# 4. Create API key in Credentials
# 5. Set up billing (required for Translation API)
# 6. Copy API key to GOOGLE_TRANSLATE_API_KEY

# ===========================================
# EXAMPLE VALUES (DO NOT USE IN PRODUCTION)
# ===========================================
# NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# GOOGLE_TRANSLATE_API_KEY=AIzaSyBvOkBwv6wK4-ZhY7P1Q2X3c4D5e6F7g8H9i0
# NEXT_PUBLIC_APP_URL=http://localhost:3000
